[project]
name = "fabricrice"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fabric",
    "psutil>=7.1.0",
    "pygobject-stubs>=2.16.0",
    "ruff>=0.14.10",
]

[tool.uv.sources]
fabric = { path = "../../Projects/fabric" }
